TRACKER_BASE_URL=https://tracker.drinkx.tech

# Файл с cookie-сессией, создаётся import_cookie.py — см. README.
# ВАЖНО: если сюда попал реальный cookie (в чат/скриншот/лог/коммит) — это
# равнозначно компрометации сессии, нужно перелогиниться в трекере (это
# отзовёт старый sid) и заново прогнать import_cookie.py.
TRACKER_STATE_PATH=./tracker_state.json

DB_PATH=./data/tracker.db

# Пусто = все доски. Можно ограничить списком через запятую, например: DEV,TEST
BOARD_CODES=
