SERVICEDESK_BASE_URL=https://sd.drinkx.tech

# Файл с cookies, создаётся login.py после разового ручного входа через
# Google SSO. НЕ коммитить — по сути это слепок живой сессии.
SERVICEDESK_STATE_PATH=./servicedesk_state.json

DB_PATH=./data/servicedesk.db

# Вложения (фото, акты, PDF) декодируются из base64 и складываются сюда,
# в БД остаётся только путь.
FILES_DIR=./data/files
